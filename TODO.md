# TODO

## Header CSS rename (case-sensitive)
- [x] Plan and inspect current Header CSS file (`header.css`) and component import (`Header.jsx`).
- [x] Rename `ecommerce-project/src/components/header.css` -> `ecommerce-project/src/components/Header.css` and update import in `Header.jsx`.
- [x] Check `git status` to see if the rename was detected (case-only change on Windows).
- [ ] If git does NOT detect, rename to a temporary name (e.g. `header2.css`), commit, then rename to `Header.css` and commit.
- [ ] Verify build/lint if needed and ensure no broken imports.
